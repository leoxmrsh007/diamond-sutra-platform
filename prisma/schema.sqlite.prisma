// Prisma 配置 - 本地开发使用 SQLite，生产使用 PostgreSQL
// Prisma Configuration - SQLite for local dev, PostgreSQL for production

import { defineConfig } from '@prisma/client';

export default defineConfig({
  // 注意：本配置仅用于类型检查和客户端生成
  // 实际数据库连接从环境变量 DATABASE_URL 读取
  // Prisma Client v7 不再需要此配置文件中的 datasource
});
